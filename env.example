# Copy to `.env.local` (recommended) or `.env` and fill in real values.
#
# NOTE: `.env` / `.env.local` are gitignored.

# ----------------------------
# Database (Azure PostgreSQL)
# ----------------------------
# Example format:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DATABASE_URL=

# ----------------------------
# Auth service (Firebase Admin)
# ----------------------------
# Option A (recommended for local): point to a service account JSON file
# e.g. FIREBASE_SERVICE_ACCOUNT_PATH=services/auth-service/firebase-service-account.json
FIREBASE_SERVICE_ACCOUNT_PATH=

# Option B: inline JSON (useful for CI)
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}
# FIREBASE_SERVICE_ACCOUNT_KEY=

# ----------------------------
# Book service (Azure placeholders)
# ----------------------------
# These are optional for starting the service, but required for Azure job/blob operations.
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=
AZURE_JOB_NAME_PREFIX=story-generation
AZURE_CONTAINER_IMAGE=

AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_CONTAINER_NAME=book-content
AZURE_STORAGE_COVER_CONTAINER=book-covers

# ----------------------------
# Local dev overrides (optional)
# ----------------------------
# AUTH_SERVICE_PORT=8001
# USER_SERVICE_PORT=8002
# BOOK_SERVICE_PORT=8003
# TRANSLATION_SERVICE_PORT=8004
# FRONTEND_PORT=5173

# If you want to override the default inter-service auth URL:
# AUTH_SERVICE_URL=http://localhost:8001


